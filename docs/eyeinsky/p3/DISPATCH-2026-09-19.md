# Orden de ejecución P3 — 2026-09-19

Alex dio la orden literal «Inicia» después de confirmar el acceso a Claude Code Opus 5. Está AUTORIZADA la construcción local P3 conforme al diseño y plan enlazados en BUILD-BRIEF. No está autorizada publicación/commit/push/merge, ni P4–P7. Los encabezados «sólo planificación», «falta orden/saldo» y «preflight bloqueado» en los documentos anteriores describen su preparación histórica; NO cancelan esta orden ni el nuevo preflight real.

## Destino único

- Worktree: `C:/Users/Alex/orca/eyeinsky-p3-opus5`.
- Rama: `eyeinsky/p3-opus5`.
- Base: `812d75c0833887069e31cc2218d47b728b9cad36`.
- App candidata: `http://127.0.0.1:4198/` (servidor separado ya iniciado por KRÓNOS).
- No tocar producto ea9e, clon principal ni preview 4197.
- Constructor único: Claude Code, modelo exacto `claude-opus-5`, high. Sin subagentes ni fallback. KRÓNOS sólo añade evidencia/activos antes del despacho y verifica después.

## Evidencia T0 obligatoria

Lee `output/eyeinsky-p3/t0/baseline-complete.json` antes de editar. El launcher sólo permite despacho si ese artefacto existe con `status: baseline-pass` y `sourceStable: true`.

- `output/eyeinsky-p3/t0/preflight.json`: nueva llamada exit0, is_error=false, respuesta OPUS5_PREFLIGHT_OK y uso efectivo claude-opus-5. También figura Haiku como uso del CLI; no afirmar que todo el CLI es exclusivamente Opus.
- `output/eyeinsky-p3/t0/backup-manifest.json` y backup.zip: 1403 entradas cotejadas SHA; nueve documentos explícitos copiados, un archivo sensible por nombre excluido. No inspeccionar la exclusión ni buscar secretos.
- Dependencias instaladas con npm ci desde lockfile en este árbol (sin compartir node_modules). npm advirtió scripts esbuild/puppeteer no aprobados; no autorizar globalmente. Build y Chrome del sistema son los verificadores reales.
- Suite medida: 4204 tests / 4194 pass / 0 fail / 10 skip. No asumir ese total cuando añadas tests. Este Node usa prefijo ℹ en sus resúmenes además de TAP #; suma todos los procesos.
- El primer comando supervisor en foreground perdió su retorno tras 420s, conservando exit0 de unit/build en archivo. No fue fallo del producto. Las confirmaciones restantes se ejecutaron separadas; logs anteriores permanecen. Una consulta diagnóstica de boundaries también agotó su timeout; sólo baseline-complete acredita el resultado final.

## Activos T3 ya preparados

- `docs/eyeinsky/p3/ASSET-LEDGER.md`: fuentes oficiales y restricciones reales revisadas.
- `output/eyeinsky-p3/t0/assets/asset-manifest.json`: metadatos/hash/bytes/dimensiones/procedencia medidos para DOS fotografías reales, sin licencia inventada.
- Derivados disponibles en `public/eyeinsky/media/p3/earth-apollo17.webp` y `earth-apollo8.webp`.
- Consume ese manifiesto para crear `src/ui/eyeinskyMediaManifest.js`; no regeneres fotos, no asignes esas fotos a vuelos. Sólo contexto `earth:view`, archivo/contextual. NASA Media Usage Guidelines es el permiso acotado a uso factual/editorial, NO CC0 ni autorización comercial universal.

## Trabajo y herramientas

Lee BUILD-BRIEF y ejecuta T1–T6 en secuencia, RED→GREEN. No pidas nuevamente aprobación del diseño: Alex ya ordenó este plan. No consumas un turno entero recitando el plan; lee código real y construye. Escribe checkpoints breves y termina con HANDOFF/result.json reales.

El shell del equipo es Git Bash sobre Windows; usa rutas nativas `C:/...` para node/git/python. Read/Glob/Grep/Edit/Write disponibles. Bash limitado a tests/build/boundaries/formato de comprobación y scripts definidos. No hay bypassPermissions ni shell irrestricto. Si formato de archivos nuevos queda fuera de permisos, informa los paths; no declares format:check aprobado por intención. KRÓNOS puede aplicar el formato acotado al candidato congelado. No inventes haber ejecutado TypeScript: lo corre el supervisor.

No modificar package-boundaries, scripts de pruebas ajenos ni umbrales para volver verdes los checks. Camera-adverse se parametriza por el contrato de T6 antes de ejecutarlo, con salida nueva. El valor por defecto sigue 4197; tus llamadas deben usar 4198 explícito.

Si el límite de turnos corta un lote, preserva estado exacto. El supervisor reanudará el MISMO session_id. Sólo escribe dentro de rutas P3 propuestas en plan y en output/eyeinsky-p3/build; no reescribas evidencia T0.
